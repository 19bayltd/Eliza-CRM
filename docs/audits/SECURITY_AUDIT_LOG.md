# Security Audit Log

Chronological record of security reviews. No phase completes with an
unresolved critical finding.

| Date | Phase | Scope | Findings | Severity | Resolution | Reviewer |
|---|---|---|---|---|---|---|
| 2026-07-31 | 00 | Full docs/ tree | No secrets, credentials, tokens, or personal data present; no code or runtime surface introduced | None | — | Implementation engineer |
| 2026-07-31 | 01 | Service-role key handling, RLS coverage, cross-company isolation, suspension blocking, audit append-only, storage privacy, secrets in repo | (1) `app` schema lacked USAGE for authenticated — fixed via migration 20260731100007, found by staging RLS probes; (2) advisors: INFO on audit_log RLS-no-policy — intended deny-all; (3) key confined to server-only module + operator script; no NEXT_PUBLIC_ leakage; .env* ignored | Low/None | (1) Fixed + verified; (2) accepted by design; (3) verified clean | Implementation engineer |
| 2026-07-31 | 01 | Owner bootstrap execution path | Service key absent from implementation environment and REST egress blocked; selected guarded management-channel SQL over a temporary Vercel endpoint (no public surface, no secret exposure, no password material — empty hash forces email-reset entry); guard against double-run verified; audit row written; production re-verified untouched (0 tables/users) | None | Executed and verified | Implementation engineer |
| 2026-07-31 | 01 | Password-reset email failure investigation | Auth logs showed 3x POST /recover -> 500 unexpected_failure ('converting NULL to string is unsupported' on confirmation_token): the SQL owner bootstrap left GoTrue token columns NULL — emails were never attempted (no SMTP handoff, no rate-limit consumption). Fixed by setting the 8 token columns to '' for the owner row; fixture script updated; audit semantics corrected so reset events record Auth-API handoff, never delivery. Secondary finding: GoTrue logged referer http://localhost:3000 on requests from Vercel egress IPs -> NEXT_PUBLIC_APP_URL on Vercel is misconfigured (owner action) | Medium (staging, single row) | Fixed + verified; app-URL fix pending owner | Implementation engineer |
| 2026-07-31 | 01 | Recovery-link otp_expired investigation | Auth logs: mail.send OK (built-in provider); first click consumed the token (successful login auth_event) but the session never reached the app. Root causes: (1) NEXT_PUBLIC_APP_URL configured with trailing slash -> redirect_to '//auth/confirm' -> middleware public-path check missed it and bounced the callback to /login, losing the one-time code; (2) default {{ .ConfirmationURL }} recovery template routes via GoTrue /verify, whose session hand-off is incompatible with server-side callbacks per Supabase docs. Fixes: env normalization strips trailing slashes; middleware normalizes multi-slash paths; /auth/confirm forwards non-secret error codes; login page explains link errors; owner updates recovery template to the documented token_hash form | Medium (staging) | Code fixed + verified; template change owner-side | Implementation engineer |
| 2026-07-31 | 01 | Recovery link otp_expired (recurrence, post-SMTP) | Fragment-format error (#error_code=otp_expired) proves failure occurs at GoTrue /verify, upstream of app code: the default ConfirmationURL template hosts the one-time link on supabase.co, and Gmail's link scanners (Google IPs in earlier logs) consume it before the human click — the docs' documented 'email prefetching' limitation. App code confirmed per SSR docs. Hardening shipped: /auth/confirm no longer consumes credentials on GET (interstitial /auth/continue + POST exchange), making our endpoint scanner-proof once the recovery template links to it with token_hash. No template/Supabase settings changed per owner instruction | Medium | Code hardened; template switch remains the required config step (owner-gated) | Implementation engineer |
| 2026-08-06 | 02–03 | Product and supplier modules: confidentiality walls, RLS parity, cross-company isolation — **backfilled 2026-08-06** | Phase 02: 2 defects (archived-product variant creation via CSV; inconsistent attribute-less variants). Phase 03: 12 findings, notably that cost-permission holders could read `supplier_quotation_costs` directly from the browser with no audit event, and child rows carried unconstrained `company_id` | All fixed: confidential tables made RLS deny-all so every read flows through the audited service; composite company-integrity FKs added; price wall re-probed (employee sees 0 cost rows) | Implementation engineer + 3 independent reviewers (Phase 03) |
| 2026-08-06 | 04 | Purchasing: approval authority, price wall, transactional integrity, function privileges | **Critical-class finding (staging only): both SECURITY DEFINER functions were executable by `anon`.** Revoking EXECUTE from anon/authenticated does not remove PostgreSQL's default grant to PUBLIC, which those roles inherit; `record_purchase_receipt` would have let an unauthenticated caller write receipts and change purchase-order status, bypassing every permission check. Found by Supabase advisors, not by the self-review. Also: over-receipt could inflate accepted quantities; request cancellation was not restricted to the requester or an approver | Revoked from PUBLIC and granted to `service_role` only; advisors re-run clean (remaining items are the intentional deny-all INFOs and the owner-action leaked-password WARN). Other findings fixed and probed. Separation of duties verified live: the Owner account was refused approval of its own request and the attempt logged with `result=failure` | Implementation engineer (self-review) + Supabase advisors |
