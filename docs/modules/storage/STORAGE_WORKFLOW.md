# Storage Module — Workflow

upload (permission + scope + validation) → active metadata + audited
download: permission + scope + path check → 300s signed URL → audited
delete: permission + scope → object removed + metadata status=deleted → audited
No permanent public URLs for private buckets, ever.
